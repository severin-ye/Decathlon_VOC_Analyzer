# 候选但未采用路线

## 引用 1

### 外链

https://github.com/openclaw/openclaw

### 对应内容整理

OpenClaw 是一个个人 AI assistant 和 agent 平台，其核心是跨渠道助手、工具调用和自动化，不是专门的多模态 RAG 文档检索框架。

### 本项目使用的部分

- 相关工作中的对比背景

### 使用的原因

原始对话中提到 OpenClaw，是为了判断它是否适合作为原始模态召回的主线方案。

### 本项目使用的思路

本项目不采用 OpenClaw 作为核心架构，但可以在论文相关工作或方案比较中说明其定位与当前项目不同。

## 引用 2

### 外链

https://github.com/openclaw/openclaw/issues/23200

### 对应内容整理

该 issue 被用来支撑一个关键判断：OpenClaw 当前并没有成熟的原生 document indexing pipeline，也没有形成面向原始模态保真的文档召回主线。

### 本项目使用的部分

- “为何不采用 OpenClaw 主线”的证据来源

### 使用的原因

它能帮助论文或答辩时解释：为什么没有选择某个看起来更像 agent 平台的方案。

### 本项目使用的思路

把它作为反向论据，说明当前项目需要的是商品证据召回和评论建模链路，而不是通用 agent 平台。

## 引用 3

### 外链

https://github.com/2dogsandanerd/ClawRag

### 对应内容整理

ClawRAG 当前更接近 Docling + chunking + Vector/BM25/RRF 的结构化或混合检索系统，多模态搜索仍是后续 roadmap。

### 本项目使用的部分

- 候选路线比较

### 使用的原因

它说明并非所有带 RAG 名称的项目都已经走到原始模态召回路线。

### 本项目使用的思路

当前项目不采用该路线作为核心，但可以在相关工作中把它作为偏文本与混合检索的对照组。
