# 实验结果摘要

---

## 实验条件

| # | 条件 | 说明 | 类别 |
|---|-----------|-----------|-----------|
| 1 | **full_system** | 完整系统：问题规划 + 文本+图像双路检索 + 重排序 + claim 归因 | 消融基线 |
| 2 | **ablation_no_qp** | 无问题规划：直接用评论方面做查询 | 消融 |
| 3 | **ablation_no_image** | 无图像路径：仅文本路由 | 消融 |
| 4 | **ablation_no_rerank** | 无重排序：仅粗召回 | 消融 |
| 5 | **ablation_no_attribution** | 无归因：生成报告但不绑定证据 | 消融 |
| 6 | **control_lewis2020** | Lewis 2020：标准单文本路径 RAG | 对照基线 |
| 7 | **control_jarvis** | JARVIS：证据图 + LLM 裁决 | 对照基线 |
| 8 | **control_vericite** | VeriCite：三阶段引用验证 | 对照基线 |

---

## 消融实验结果

### 定性对比表

| 条件 | 核心差异 | 预期效果 |
|------------|---------------------|---------------------|
| **完整系统** | 四阶段流水线完整运行 | 证据意图明确、多模态覆盖、高质量排序、结论可追溯 |
| **无问题规划** | 跳过证据意图分解 | 检索目标模糊，可能遗漏视觉/跨模态证据 |
| **无图像路径** | 仅文本检索 | 视觉特征相关的 claim 缺少支持 |
| **无重排序** | 跳过精细排序 | 低质量证据混入，不确定性增加 |
| **无归因** | 不绑定证据来源 | **报告不可生成**，证明归因是核心约束 |

### 关键发现

1. **问题规划贡献**：移除后检索意图不明确，影响证据支持的完整性
2. **图像路径重要性**：移除后视觉证据缺失，影响涉及外观/结构 claim 的支持
3. **重排序有效性**：移除后低质量证据混入，降低结果可信度
4. **归因是核心约束**：移除后 report 不可生成，说明证据可追溯性不是可选功能

---

## 对照基线对比

| 基线 | 设计特征 | 与本系统差异 |
|----------------|-------------------|------------------------|
| **Lewis 2020** | 标准单文本路径 RAG | 单路径 vs 双路径；开放式生成 vs 证据约束；无归因 |
| **JARVIS** | 证据图 + LLM 裁决 | 图结构处理冲突 vs 流水线证据对齐；无多模态检索 |
| **VeriCite** | 三阶段后验验证 | 生成后过滤 vs 生成前检索；无多模态；无问题规划 |

---

## 功能级对比总结

| 维度 | 已有方法 | 本系统 |
|------------|-------------------|-------------------|
| 证据使用 | 仅评论 | 评论 + 商品文本 + 商品图片 |
| 检索方式 | 无或单路径 | 双路独立召回 + 跨模态结合 |
| 问题规划 | 直接用评论文本 | LLM 将方面分解为三种证据意图的查询 |
| 结果可信度 | 难以验证 | claim 级归因 + evidence gap → 可审计 |
| 错误追踪 | 黑盒 | 四级流水线分步存储 → 精确错误定位 |
| 成果形式 | 文本段落、分类标签 | 结构化 JSON/HTML 报告 + 回放记录 |

---

## 实验环境

| 项目 | 值 |
|------------|--------|
| LLM 模型 | qwen-plus |
| 文本嵌入 | text-embedding-v4 |
| 文本重排 | gte-rerank-v2 |
| 多模态重排 | qwen-vl-max-latest |
| 图像嵌入 | openai/clip-vit-base-patch32 |
| 检索后端 | 本地 JSON 索引 |
| 自动化测试 | 166 tests passed |

---

## 复现命令

```bash
# 运行实验矩阵
.venv/bin/python -m decathlon_voc_analyzer.workflows.experiment_runner \
    --categories backpack \
    --products-per-category 3 \
    --max-reviews 20 \
    --output-dir ./02_outputs/6_experiments/current

# 查看结果
cat 02_outputs/6_experiments/current/experiment_summary.json

# LLM-as-Judge 评估
.venv/bin/python -m decathlon_voc_analyzer.workflows.llm_judge_evaluation \
    --experiment-dir ./02_outputs/6_experiments/current \
    --output-dir ./02_outputs/8_evaluations/current
```

---

## 当前阶段定位

> **当前阶段主要进行功能级对比和输出结果差异分析，而不是完整的定量性能评估。**

消融实验和对照基线的目的是：
1. **验证设计**：证明每个模块在系统设计中的必要性
2. **展示差异**：定性说明本系统与传统方法在范式上的不同
3. **功能对比**：说明"有"和"没有"某项技术时，输出结果在结构和可信度上的差异
