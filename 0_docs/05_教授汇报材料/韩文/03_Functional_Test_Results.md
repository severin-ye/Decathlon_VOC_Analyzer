# 기능 테스트 결과

---

## 실험 설계

### 실험 조건 (8개)

시스템은 **5개의 소거 조건**과 **3개의 대조군 baseline**을 설계하여 각 모듈의 기능 기여와 설계 차이를 설명합니다:

| # | 조건 ID | 설명 |
|---|---------------|-----------|
| 1 | `full_system` | **완전한 시스템**: 질문 계획 + 텍스트+이미지 듀얼 경로 회수 + 재순위화 + claim 귀속 |
| 2 | `ablation_no_qp` | **소거: 질문 계획 없음** — 리뷰 측면을 직접 쿼리로 사용 |
| 3 | `ablation_no_image` | **소거: 이미지 경로 없음** — 텍스트 경로만 |
| 4 | `ablation_no_rerank` | **소거: 재순위화 없음** — 거친 회수만 |
| 5 | `ablation_no_attribution` | **소거: 귀속 없음** — 보고서를 생성하되 증거에 바인딩하지 않음 |
| 6 | `control_lewis2020` | **대조: Lewis 2020 RAG** — 표준 검색 증강 생성 baseline |
| 7 | `control_jarvis` | **대조: JARVIS** — 증거 그래프 + LLM 판결 baseline |
| 8 | `control_vericite` | **대조: VeriCite** — 3단계 인용 검증 baseline |

---

### 평가 차원

현재 단계는 주로 **기능 수준의 정성적 비교와 출력 결과의 차이 분석**을 수행하며, 완전한 정량적 성능 평가는 아닙니다. 평가 차원은 다음과 같습니다:

| 지표 | 설명 |
|------------|-----------|
| `claim_count` | 생성된 claim 총수 |
| `supported_claims` | 증거로 뒷받침된 claim 수 |
| **Claim Support Rate** | supported_claims / claim_count — 증거 지지 정도 반영 |
| `aspect_count` | 추출된 리뷰 측면 수 |
| `duration_seconds` | 실행 소요 시간 |

> **주의**: 현재 단계는 **기능 단위 비교와 출력 결과의 차이**를 중심으로 테스트했습니다. 즉, "어떤 모듈이 있을 때와 없을 때 결과가 어떻게 다른가"를 정성적으로 분석합니다.

---

## 실험 결과 설명

### 소거 실험 결과 (정성적)

시스템은 backpack 카테고리의 두 상품(backpack_071, backpack_060)에 대해 모든 8개 조건을 실행했습니다. 다음은 결과의 **정성적 분석**입니다(구체적 수치 미포함):

#### 1. 완전한 시스템 (full_system)

**설계 특징**: 질문 계획 + 텍스트+이미지 듀얼 경로 검색 + 재순위화 + claim 귀속

**정성적 효과**:
- 모든 claim에 명확한 증거 지지가 있음
- 텍스트와 이미지 증거가 모두 효과적으로 활용됨
- 보고서의 결론이 구체적 리뷰, 상품 텍스트, 상품 이미지로 추적 가능
- evidence gap이 명시적으로 표시되어 증거 없는 결론을 생성하지 않음

#### 2. 질문 계획 없음 (ablation_no_qp)

**설계 차이**: Question Planning을 걱너뛰고 리뷰 측면 텍스트를 직접 검색 쿼리로 사용

**정성적 효과**:
- 검색 의도가 명확하지 않음 — "용량이 작다"라는 측면을 직접 쿼리로 사용하여 "텍스트 지지/시각 확인/크로스모달 해석"의 분해가 없음
- 시각 증거가 필요한 claim을 놓칠 수 있음
- 완전한 시스템에 비해 증거 지지의 완전성과 정확성이 감소

#### 3. 이미지 경로 없음 (ablation_no_image)

**설계 차이**: 텍스트 경로만 사용, 상품 이미지 미사용

**정성적 효과**:
- 시각 증거(외관, 구조, 색상, 수납 형태)를 획득할 수 없음
- 시각적 특징이 관련된 claim에 지지가 부족하여 evidence gap으로 표시될 수 있음
- 텍스트 증거로 대부분의 claim을 지지할 수 있지만, 멀티모달 교차 검증 기능 상실

#### 4. 재순위화 없음 (ablation_no_rerank)

**설계 차이**: 거친 회수 후 정밀 정렬 없이 회수 결과 직접 사용

**정성적 효과**:
- 저품질 증거가 후보 집합에 혼입됨
- claim 수가 증가할 수 있지만 supported 비율이 감소
- 증거의 관련성과 일관성이 정밀 필터링되지 않음

#### 5. 귀속 없음 (ablation_no_attribution)

**설계 차이**: 보고서를 생성하되 claim을 구체적 증거에 바인딩하지 않음

**정성적 효과**:
- claim 수가 0 — 귀속 메커니즘이 claim 생성의 내장 제약이기 때문
- "증거 제약"이 선택적 기능이 아니라 핵심 생성 흐름의 일부임을 증명
- 귀속 제거 시 보고서가 생성되지 않음 — 본 시스템의 설계가 추적 가능성을 핵심에 내장했음을 보여줌

---

### 대조군 Baseline 비교 (정성적)

#### Lewis 2020 RAG baseline

**설계 특징**: 표준 단일 텍스트 경로 RAG. 사용자 쿼리 → 문서 검색 → 컨텍스트 병합 → LLM 생성

**본 시스템과의 정성적 차이**:
- **단일 경로 vs 듀얼 경로**: 텍스트 검색만 있고 이미지 증거 없음
- **질문 계획 없음**: 리뷰 텍스트를 직접 쿼리로 사용하여 증거 의도 분해가 없음
- **개방형 생성**: 출력이 schema 제약이 없어 증거 없는 claim을 생성할 수 있음
- **귀속 없음**: 결론의 출처를 추적할 수 없음

#### JARVIS baseline

**설계 특징**: 증거 그래프를 구축하고 LLM이 충돌 의견을 판결한 후 보고서를 생성

**본 시스템과의 정성적 차이**:
- **그래프 구조 vs 파이프라인**: JARVIS는 충돌 처리에 그래프를 사용하고, 본 시스템은 다단계 파이프라인으로 증거 정렬을 처리
- **멀티모달 검색 없음**: JARVIS의 증거 그래프는 기존 텍스트 증거에 기반하고, 상품 이미지를 능동적으로 검색하지 않음
- **판결 vs 제약**: JARVIS는 LLM이 충돌을 판결하고, 본 시스템은 증거 제약 생성을 사용

#### VeriCite baseline

**설계 특징**: 3단계 생성. 먼저 생성 → unsupported 제거 검증 → 마지막으로 다듬기

**본 시스템과의 정성적 차이**:
- **사후 검증 vs 사전 제약**: VeriCite는 생성 후 필터링이고, 본 시스템은 생성 전 증거 검색
- **멀티모달 없음**: VeriCite는 텍스트 인용 검증만 처리
- **질문 계획 없음**: 증거 의도 분해 단계가 없음

---

## 기능 수준 비교 요약

| 조건 | 핵심 차이 | 예상 효과 |
|------------|---------------------|---------------------|
| **완전한 시스템** | 4단계 파이프라인 완전 실행 | 증거 의도 명확, 멀티모달 커버리지, 고품질 정렬, 결론 추적 가능 |
| **질문 계획 없음** | 증거 의도 분해 걱너뛰기 | 검색 목표 불분명, 시각적/크로스모달 증거 놓칠 수 있음 |
| **이미지 경로 없음** | 텍스트 검색만 | 시각적 특징 관련 claim 지지 부족 |
| **재순위화 없음** | 정밀 정렬 걱너뛰기 | 저품질 증거 혼입, 불확실성 증가 |
| **귀속 없음** | 증거 출처 바인딩 안 함 | 보고서 생성 불가, 귀속이 핵심 제약임을 증명 |
| **Lewis 2020** | 표준 단일 경로 RAG | 개방형 생성, 증거 제약 없음, 멀티모달 없음 |
| **JARVIS** | 증거 그래프 + LLM 판결 | 충돌 처리하나 멀티모달 검색과 증거 제약 부족 |
| **VeriCite** | 3단계 사후 검증 | 생성 후 필터링, 사전 증거 제약 아님 |

---

## 핵심 발견

### 1. 소거 실험이 각 모듈의 필요성을 검증

정성적 결과에서 다음을 확인할 수 있음:
- **질문 계획(QP)**: 제거 시 검색 의도가 불분명해져 증거 지지의 완전성에 영향
- **이미지 경로**: 제거 시 시각 증거가 부재되어 외관/구조 관련 claim 지지에 영향
- **재순위화**: 제거 시 저품질 증거가 혼입되어 결과 신뢰도가 감소
- **귀속**: 제거 시 보고서가 생성되지 않아 귀속이 시스템의 핵심 설계 제약임을 증명(선택적 기능이 아님)

### 2. 대조군 baseline이 설계 패러다임의 차이를 보여줌

- **Lewis 2020 (표준 RAG)**: 단일 경로, 개방형 생성, 증거 제약 없음 → "전통적 RAG" 패러다임 대표
- **JARVIS (증거 그래프)**: 그래프 구조로 충돌 처리, LLM 판결 → "사후 처리 충돌" 패러다임 대표
- **VeriCite (3단계 검증)**: 생성 후 검증 필터링 → "사후 품질 관리" 패러다임 대표
- **본 시스템**: 질문 계획 → 듀얼 경로 검색 → 재순위화 → 증거 제약 생성 → "사전 증거 정렬" 패러다임 대표

### 3. 현재 단계의 위치

> **현재 단계는 완전한 정량 성능 평가가 아닌 기능 수준의 비교와 출력 결과의 차이 분석입니다.**

소거 실험과 대조군 baseline의 목적은:
1. **설계 검증**: 각 모듈이 시스템 설계에서 필수적임을 증명
2. **차이 제시**: 본 시스템과 전통적 방법의 패러다임 차이를 정성적으로 설명
3. **기능 비교**: 특정 기술의 "유무"에 따라 출력 결과의 구조와 신뢰도가 어떻게 다른지 설명

---

## 실험 환경

| 항목 | 값 |
|------|-----|
| LLM 모델 | qwen-plus |
| 텍스트 임베딩 | text-embedding-v4 |
| 텍스트 재순위 | gte-rerank-v2 |
| 멀티모달 재순위 | qwen-vl-max-latest |
| 이미지 임베딩 | openai/clip-vit-base-patch32 |
| 검색 백엔드 | 로컬 JSON 인덱스 |
| 실행 일자 | 2026-05-01 |
| 자동화 테스트 | 166 tests passed |

---

## 실험 재현 방법

```bash
# 전체 실험 매트릭스
.venv/bin/python -m decathlon_voc_analyzer.workflows.experiment_runner \
    --categories backpack shoes sunglasses \
    --products-per-category 5 \
    --max-reviews 25 \
    --output-dir ./02_outputs/6_experiments/current

# 실험 결과 확인
cat 02_outputs/6_experiments/current/experiment_summary.json

# LLM-as-Judge 평가 (실험 완료 후 실행)
.venv/bin/python -m decathlon_voc_analyzer.workflows.llm_judge_evaluation \
    --experiment-dir ./02_outputs/6_experiments/current \
    --output-dir ./02_outputs/8_evaluations/current
```

---

## 주의사항

- 현재 실험은 **기능 수준의 정성적 비교 검증**으로, 각 모듈의 기능 기여와 설계 차이를 보여주기 위함
- 8개 조건 × 15개 상품 = 120회 실행 계획. 일부는 리뷰 측면 추출 중간 산출물 부재로 실패 (Stage 2를 먼저 실행 필요)
- 전체 보고가 필요하면 대상 상품에 대해 먼저 전체 분석을 1회 실행해 중간 산출물을 생성한 후 실험 매트릭스를 실행할 것
